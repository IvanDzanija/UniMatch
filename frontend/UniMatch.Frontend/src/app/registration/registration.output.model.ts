import { NumberSymbol } from "@angular/common";

export type Registration = {
    id: number;
    username: string;
    email: string;
    password: string;
}
export interface User {
    id: number;
    username: string;
    email: string;
    password: string;
}